# Architecture

The project follows a feature-based architecture.

Each feature contains its own:

- Controller
- Service
- Repository
- Entity
- DTO
- Mapper

Shared components will be placed inside the common package.

The objective is to maximize cohesion and minimize coupling.