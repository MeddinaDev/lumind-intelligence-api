spring:
  datasource:
    url: jdbc:h2:mem:lumind_test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

  flyway:
    enabled: false

jwt:
  secret: MDEyMzQ1Njc4OUFCRUNERjAxMjM0NTY3ODlBQkNERUY=
  issuer: lumind-intelligence-api-test
  access-token-expiration: 900
  refresh-token-expiration: 604800
